@model List<Album>
@{
    ViewData["Title"] = "Index";
}

<br />
<h1 class="offset-2">Index</h1>
<hr />
<br />

<table class="offset-2">
    <thead>
        <tr>
            <th>Title</th>
            <th>Creator</th>
            <th>Options</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count != 0)
        {

            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Title</td>
                    <td>@item.AlbumCreator</td>
                    <td>
                        <partial name="_EDDButtons" />
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td><p>There's no Albums available.</p></td>
            </tr>
        }
    </tbody>
</table>

<a asp-action="CreateAlbum" asp-controller="Album" class="btn btn-success">Create new Album</a>